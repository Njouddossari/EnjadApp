<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.enjad">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    //To get user location(turn the gps ON)
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/enjadapplogoicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".ProfileActivity"  android:icon="@drawable/enjadapplogoicon" />
        <activity android:name=".chatlistActivity" android:icon="@drawable/enjadapplogoicon"/>
        <activity android:name=".HomPageActivity" android:icon="@drawable/enjadapplogoicon"/>
        <activity android:name=".ReportlistActivity" android:icon="@drawable/enjadapplogoicon"/>
        <activity android:name=".MainActivity" android:icon="@drawable/enjadapplogoicon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <uses-permission android:name="android.permission.GET_TASKS" />
        </activity>
    </application>

</manifest>