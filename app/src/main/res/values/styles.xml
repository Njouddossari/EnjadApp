<resources>

    <!-- Base application theme.
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"> -->
    <style name="CustomeToolBar" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:background">@color/colorPrimaryBlack</item>
        <item name="android:textColor">@color/whitetext</item>
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-4dp</item>
        <item name="logo">@drawable/enjadapplogoicon</item>
        <item name="displayOptions">useLogo|showHome</item>

    </style>
        <!-- Customize your theme here. -->
      <!-- <style name="CustomeToolBar" parent="Theme.AppCompat.Light.NoActionBar"> *****to be deleted after checking


    </style>-->
    <style name="ButtonText" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/whitetext</item>
        <item name="android:textSize">22sp</item>
       <!-- <item name="fontFamily">Caviar Dreams</item>-->
        <item name="android:background">@drawable/button_desing</item><!-- only way to change this is to change the src image -->
        <item name="android:layout_width">80.0px</item>
        <item name="android:layout_height">40.0px</item>
    </style>


</resources>
